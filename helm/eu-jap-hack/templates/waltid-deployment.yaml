---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eu-jap-hack.fullname" . }}-waltid-wallet
  labels:
    {{- include "eu-jap-hack.labels" . | nindent 4 }}
    app.kubernetes.io/component: waltid-wallet-api
spec:
  replicas: {{ .Values.waltidWalletApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: waltid-wallet-api
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: waltid-wallet-api
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: waltid-wallet-api
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: waltid-wallet-api
          image: {{ include "eu-jap-hack.image" (dict "global" .Values.global "image" .Values.waltidWalletApi.image) }}
          imagePullPolicy: {{ .Values.waltidWalletApi.image.pullPolicy }}
          ports:
            - containerPort: 7001
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /waltid-wallet-api/config/db.conf
              subPath: db.conf
            - name: config
              mountPath: /waltid-wallet-api/config/auth.conf
              subPath: auth.conf
            - name: config
              mountPath: /waltid-wallet-api/config/web.conf
              subPath: web.conf
          resources:
            {{- toYaml .Values.waltidWalletApi.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "eu-jap-hack.fullname" . }}-waltid-wallet
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eu-jap-hack.fullname" . }}-waltid-issuer
  labels:
    {{- include "eu-jap-hack.labels" . | nindent 4 }}
    app.kubernetes.io/component: waltid-issuer-api
spec:
  replicas: {{ .Values.waltidIssuerApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: waltid-issuer-api
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: waltid-issuer-api
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: waltid-issuer-api
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: waltid-issuer-api
          image: {{ include "eu-jap-hack.image" (dict "global" .Values.global "image" .Values.waltidIssuerApi.image) }}
          imagePullPolicy: {{ .Values.waltidIssuerApi.image.pullPolicy }}
          ports:
            - containerPort: 7002
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /waltid-issuer-api/config/issuer-service.conf
              subPath: issuer-service.conf
            - name: config
              mountPath: /waltid-issuer-api/config/web.conf
              subPath: web.conf
            - name: config
              mountPath: /waltid-issuer-api/config/credential-issuer-metadata.conf
              subPath: credential-issuer-metadata.conf
          resources:
            {{- toYaml .Values.waltidIssuerApi.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "eu-jap-hack.fullname" . }}-waltid-issuer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eu-jap-hack.fullname" . }}-waltid-verifier
  labels:
    {{- include "eu-jap-hack.labels" . | nindent 4 }}
    app.kubernetes.io/component: waltid-verifier-api
spec:
  replicas: {{ .Values.waltidVerifierApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: waltid-verifier-api
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: waltid-verifier-api
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: waltid-verifier-api
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: waltid-verifier-api
          image: {{ include "eu-jap-hack.image" (dict "global" .Values.global "image" .Values.waltidVerifierApi.image) }}
          imagePullPolicy: {{ .Values.waltidVerifierApi.image.pullPolicy }}
          ports:
            - containerPort: 7003
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /waltid-verifier-api/config/verifier-service.conf
              subPath: verifier-service.conf
            - name: config
              mountPath: /waltid-verifier-api/config/web.conf
              subPath: web.conf
          resources:
            {{- toYaml .Values.waltidVerifierApi.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "eu-jap-hack.fullname" . }}-waltid-verifier
